# 2026-09-17

## 重命名：score.* → index.*

- 需求：把入口页 `score.html` 改为 `index.html`，并排查连带影响。
- 用户选择方案：三件套统一改名（`index.html` / `index.css` / `index.js`）。
- 执行内容：
  - 重命名三个文件，并同步修改 `index.html` 内第 7 行样式表引用、第 69 行脚本引用（含 `?v=20260917b` 缓存戳）。
  - 全量扫描确认项目内无对旧文件名的其它引用（无 `<a href>`、无 JS 跳转）。
  - 备份原文件为 `score.*.bak`，并新增 `.gitignore` 忽略 `*.bak`。
  - Git 以重命名方式提交（`R100`/`R096`/`R100`），commit `8740b96`，历史链完整保留。
- 验证方式：本地 HTTP 服务探测全部资源均 200；Node 桩环境实际执行 `index.js`，确认 DOM 绑定有效——53 门课程、5 条规则、总 GPA 3.51、7/7 课程区块渲染正常。
- 关键结论：改名不影响任何功能；`.bak` 备份文件目前保留在磁盘上，确认无误后可删除。

## 项目结构现状

- 纯静态站点，无构建步骤，无 package.json。
- 数据层：`data/rules.csv`（规章制度）、`data/summary.csv`（键值对统计）、`data/courses.csv`（课程明细）。
- `index.js` 的 `loadCSV()` 已内置 UTF-8/UTF-8 BOM/GBK/UTF-16 编码嗅探，并对 CSV 追加时间戳绕过缓存；`data/courses.csv.gbk.bak` 是历史上 GBK 编码问题的遗留备份。
- 因数据通过 `fetch` 读取本地 CSV，**必须经 HTTP 服务访问，直接双击打开会因 CORS/`file://` 限制导致数据加载失败**。
